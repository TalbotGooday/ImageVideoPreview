<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.exoplayer2.ui.PlayerView
        android:id="@+id/player_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:resize_mode="fixed_width"
        app:shutter_background_color="@color/mediaViewBackgroundColor"
        app:use_controller="false"/>


    <com.goodayapps.widget.media_viewer.utils.widgets.progress.MvProgress
        android:id="@+id/video_progress"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_gravity="center"
        android:padding="8dp"
        android:background="@drawable/ic_player_progress"
        app:mvpg_indeterminate="true"
        app:mvpg_progress="40"
        app:mvpg_rimColor="#ffffff"
        app:mvpg_rimWidth="3dp"
        app:mvpg_showProgress="true"
        app:mvpg_speed="1"
        tools:visibility="visible" />

    <ImageView
        android:id="@+id/play_pause"
        android:layout_width="@dimen/play_pause_size"
        android:layout_height="@dimen/play_pause_size"
        android:layout_gravity="center"
        android:contentDescription="@null"
        tools:srcCompat="@drawable/ic_player_play" />

</FrameLayout>